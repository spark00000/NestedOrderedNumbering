# Variable-prefix hanging-indent manual vector

Use a narrow editor pane so every item wraps to at least two visual lines.
The second and later visual lines must start exactly under the first content
character after the complete numbering prefix.

1. Root item with enough text to wrap onto another visual line and verify root alignment.
10. Two-digit root item with enough text to wrap onto another visual line and verify root alignment.
  10.1. Nested item with enough text to wrap onto another visual line and verify nested alignment.
    10.1.1. Deeper item with enough text to wrap onto another visual line and verify deeper alignment.
      10.12.3. Multi-digit segments with enough text to wrap onto another visual line and verify dynamic prefix width.
